/* FILE # 2 - Implemenation File
**	Defines member functions
*/
#include "cisCircle.h"
using namespace std;

	CisCircle::CisCircle() {
	}

	CisCircle::CisCircle(const CisCircle& arg) {
		radius = arg.radius;
	}
	
	CisFraction computeArea(void);
		return CisFraction(3) * radius * radius;
		}

	int compareCircle(const CisCircle&);